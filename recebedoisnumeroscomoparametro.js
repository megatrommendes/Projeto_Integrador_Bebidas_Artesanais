function operacoesBasicas(a, b) {
  console.log(`${a} + ${b} = ${a + b}`);
  console.log(`${a} - ${b} = ${a - b}`);
  console.log(`${a} x ${b} = ${a * b}`);
  console.log(`${a} / ${b} = ${a / b}`);
}

// Exemplo de uso da função com os números 4 e 5
operacoesBasicas(4, 5);